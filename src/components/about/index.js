import React from 'react';

export default function About() {
    
    const contentStyle = {
      textAlign: 'center',
      minHeight: 120,
      lineHeight: '120px',
      color: '#fff',
      backgroundColor: '#108ee9',
    };

    return (
        <div style={contentStyle}>
            <h1>About</h1>
        </div>
    );
}